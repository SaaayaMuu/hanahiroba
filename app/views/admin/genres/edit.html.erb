<%= render 'layouts/admin-header' %>
<div class="container">

  <h3 class="mb-3 mt-3">ジャンル編集</h3>

  <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
    <%= f.text_field :name, autofocus: true, required: true %>
    <%= f.submit '保存' %>
  <% end %>

</div>